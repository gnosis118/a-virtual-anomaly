
import React from 'react';
import { useDevMode } from '@/contexts/DevModeContext';

const DevModeToggle = () => {
  // Always return null to completely remove the dev button
  return null;
};

export default DevModeToggle;
